# pldf-models

Shared PLDF data models and DTO-like contracts used by features.

Public API policy:
- Export shared model descriptors via `src/index.js`.
- Any breaking model change requires version note in architecture docs.
