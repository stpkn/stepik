# common-utils

Shared utility functions (data formatting, validation helpers, mapping utilities).

Public API policy:
- Export only through `src/index.js`.
- Keep side effects out of utility modules.
